<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <parent>
    <groupId>io.github.fragudev.ailab</groupId>
    <artifactId>ai-engineering-lab</artifactId>
    <version>0.1.0-SNAPSHOT</version>
    <relativePath>../../pom.xml</relativePath>
  </parent>

  <artifactId>ingestion</artifactId>
  <packaging>jar</packaging>

  <name>AI Engineering Lab :: ingestion</name>
  <description>Document lifecycle, ingestion jobs, and Kafka producers/consumers.</description>

  <dependencies>
    <dependency>
      <groupId>io.github.fragudev.ailab</groupId>
      <artifactId>shared</artifactId>
    </dependency>
    <dependency>
      <groupId>io.github.fragudev.ailab</groupId>
      <artifactId>ai-provider</artifactId>
    </dependency>
    <dependency>
      <groupId>io.github.fragudev.ailab</groupId>
      <artifactId>knowledge</artifactId>
    </dependency>
    <dependency>
      <groupId>io.github.fragudev.ailab</groupId>
      <artifactId>platform</artifactId>
    </dependency>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-data-jpa</artifactId>
    </dependency>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-kafka</artifactId>
    </dependency>
    <dependency>
      <groupId>org.springframework.modulith</groupId>
      <artifactId>spring-modulith-events-api</artifactId>
    </dependency>
    <dependency>
      <groupId>org.springframework.modulith</groupId>
      <artifactId>spring-modulith-events-kafka</artifactId>
    </dependency>
    <!-- Tags each stage's active span with correlationId (docs/adr/0005-kafka.md, criterion 5:
         the whole ingestion flow visible as one trace by filtering on it). Brought transitively by
         app's spring-boot-starter-opentelemetry, declared explicitly here since this module's own
         code references the Tracer type directly. -->
    <dependency>
      <groupId>io.micrometer</groupId>
      <artifactId>micrometer-tracing</artifactId>
    </dependency>
    <dependency>
      <groupId>org.junit.jupiter</groupId>
      <artifactId>junit-jupiter</artifactId>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>org.assertj</groupId>
      <artifactId>assertj-core</artifactId>
      <scope>test</scope>
    </dependency>
  </dependencies>
</project>
