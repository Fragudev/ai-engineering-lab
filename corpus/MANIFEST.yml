# Demo corpus manifest
#
# Read by scripts/fetch-corpus.sh. Third-party documents are downloaded rather than
# committed, so this file is the record of what the corpus actually contains.
#
# Status: placeholder. Ingestion exists as of Phase 2, but populating this file is a demo-corpus/
# seed-script concern (scripts/fetch-corpus.sh, part of the README's getting-started flow) — deferred
# to Phase 3, when the demo corpus is actually needed for RAG (docs/roadmap.md).
# Every entry's license is verified at fetch time, not assumed from this file.

version: 0
retrieved_at: null # set by fetch-corpus.sh

sources: []
# Shape of an entry, for reference:
#
# - id: spring-boot-reference
#   title: Spring Boot Reference Documentation
#   url: https://...
#   license: Apache-2.0
#   license_url: https://...
#   sha256: null          # recorded at fetch time; makes the corpus reproducible
#   tags: [spring, framework]
